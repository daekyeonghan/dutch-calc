# dutch-calc — MVP 스펙

## 한 줄 정의
지출 **항목 리스트를 통째로 붙여넣으면** 자동으로 합산·N등분해서 "누가 누구에게 얼마 보내면 되는지"까지 계산해주는 무료 정산기.

**차별점:** 토스/카카오페이는 사람당 *최종 금액 하나*를 숫자로 입력해야 함(먼저 다 더해야 함). dutch-calc는 그 "먼저 더하는 노동"을 없앰. 타겟 = 복잡한 공동지출을 주기적으로 정산하는 사람(커플·룸메·자주 같이 쓰는 사이).

## 핵심 흐름
1. 인원·이름 입력 (예: 2명 → A, B)
2. 사람별 텍스트박스에 지출 리스트 붙여넣기 — 한 줄 = `항목 금액`
3. 파싱 — 코드 파서가 각 줄을 `{항목, 금액}`으로 분해 (**LLM 없음**)
4. 확인·수정 화면 — 사람별 항목/합계 확인, 금액 수정·항목 삭제/추가 (**신뢰의 핵심**)
5. 정산 계산 — 총액, 1인당 부담액, 각자 잔액, 최종 송금 지시("B가 A에게 X원")
6. 기록 저장 — 결과가 내 정산 기록에 남음 (Phase 2)

## 계산 로직 (전부 결정적 코드)
- 각 사람 `낸돈 = 항목 합계`, `총액 = Σ낸돈`, `1인당 = 총액 / N`
- `잔액 = 낸돈 − 1인당 부담액` (양수=받을 돈 / 음수=줄 돈)
- 송금: 채무자→채권자 매칭 (2명=1건, 3명+=송금 건수 최소화)
- **자투리 원**: 1인당을 반올림 단위로 맞추며 생기는 잔여를 **랜덤 한 명에게 배정하고 화면에 명시**.
  이 규칙으로 *받을 돈 합계 = 줄 돈 합계* 항상 정확히 일치.
- **반올림 옵션**: 원/십원 단위 · 반올림/내림 (기본값: **원 단위 반올림**)

## 파싱 규칙
- 각 줄 = 한 항목. 금액 = 줄의 마지막 숫자 토큰(쉼표 제거: `53,200`→`53200`), 나머지 = 항목명(선택)
- 금액 못 찾은 줄 → "인식 실패"로 표시, 확인 화면에서 수정/삭제
- 엉망 입력(“대충 3만”, “만오천”)은 지원 안 함 — 무료 서비스, 규칙적 입력 전제

## 저장 (Phase 2)
- 브라우저 익명 ID(localStorage UUID → `X-Client-Id` 헤더). 로그인은 나중에.
- Postgres에 clientId로 저장(localStorage엔 열쇠만). 나중에 로그인 붙일 때 clientId→userId로 교체.
- `settlements(id, client_id, title, people_count, total, payload JSONB, created_at)` — 상세는 payload(JSONB)에.

## 기술 스택
- Next.js(App Router) + TypeScript + Tailwind CSS
- 파싱·계산 = 순수 TS 함수(`lib/calc.ts`), 단위 테스트
- 저장/조회 = Route Handlers(`app/api/...`)
- PostgreSQL(**Neon**) + **Prisma**
- 배포: **Vercel**

## MVP 비범위
LLM 파싱 · 로그인/계정 · 부분 분할(일부만 나눠 갖는 항목) · 실제 송금 연동 · 다중 통화(원화 고정)
