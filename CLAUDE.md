@AGENTS.md

# dutch-calc — 프로젝트 브리핑

무료 더치페이 정산기. 지출 **항목 리스트를 통째로 붙여넣으면** 파싱→합산→N등분→"누가 누구에게 얼마" 계산.
차별점: 토스/카카오페이는 사람당 최종 금액 하나를 숫자로 입력해야 함(먼저 다 더해야 함). 이 앱은 그 노동을 없앰.

- 전체 스펙·결정 사항: **`SPEC.md`** 참고 (반드시 먼저 읽기)
- 핵심 원칙: **파싱·계산은 결정적 코드**(LLM 안 씀), 돈 계산이라 정확성 최우선
- 스택: Next.js(App Router) + TS + Tailwind v4 / DB는 Neon(Postgres) + Prisma / 배포 Vercel
- 저장은 로그인 없이 **브라우저 익명 ID(localStorage → X-Client-Id 헤더)**, 로그인은 나중에

## 진행 상황
- ✅ Phase 1: 계산기 (입력→파싱→확인/수정→결과). `lib/calc.ts`(순수 로직) + `app/calculator.tsx`(4단계 UI). 실제 예시로 검증됨.
- ⬜ 단위 테스트(vitest) — 돈 계산 자동 검증
- ⬜ Phase 2: Prisma + Neon, `settlements` 저장/조회 API, 정산 기록 화면
- ⬜ Vercel 배포

## 계산 규칙 요약 (lib/calc.ts)
- 각 사람 낸돈=항목합, 총액/N=1인당, 잔액=낸돈−몫(양수 받을 돈/음수 줄 돈)
- 자투리(반올림 차액)는 랜덤 한 명에게 몰아 총합을 정확히 맞춤 + 화면에 명시
- 반올림 옵션: 원/십원 · 반올림/내림 (기본 원 단위 반올림)
